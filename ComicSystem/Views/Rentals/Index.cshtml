@model IEnumerable<ComicSystem.Models.Rental>

@{
    ViewData["Title"] = "Danh sách phiếu thuê";
}

<h2>Danh sách phiếu thuê</h2>

<table class="table">
    <thead>
        <tr>
            <th>Khách hàng</th>
            <th>Ngày thuê</th>
            <th>Ngày trả</th>
            <th>Trạng thái</th>
            <th>Hành động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var rental in Model)
        {
            <tr>
                <td>@rental.Customer?.FullName</td>
                <td>@rental.RentalDate.ToShortDateString()</td>
                <td>@rental.ReturnDate.ToShortDateString()</td>
                <td>@rental.Status</td>
                <td>
                    <a href="/Rentals/Details/@rental.RentalID" class="btn btn-primary">Chi tiết</a>
                    <a href="/Rentals/Edit/@rental.RentalID" class="btn btn-warning">Sửa</a>
                    <a href="/Rentals/Delete/@rental.RentalID" class="btn btn-danger">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="/Rentals/Create" class="btn btn-success">Thêm phiếu thuê</a>
